[bdist_wheel]
# This is not universal (it only support python 3)
universal=0

[versioneer]
VCS = git
style = pep440
versionfile_source = docker_ds/_version.py
versionfile_build = docker_ds/_version.py
tag_prefix =
parentdir_prefix = docker_ds-
